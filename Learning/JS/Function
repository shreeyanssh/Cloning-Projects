->JS has a standard version that most browsers follow called Es or ECMAscript.
->The function in es6 are of one type futher divider into 3 types.i.e.
     Fat Arrow Function
   a)Basic Fat Arrow
   b)Fat Arrow with one function
   c)Fat Arrow with implicit return
-> 